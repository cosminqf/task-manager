@{
    var icon = ViewBag.Icon ?? "📭";
    var title = ViewBag.Title ?? "Niciun element găsit";
    var description = ViewBag.Description ?? "Nu există încă elemente de afișat.";
    var actionText = ViewBag.ActionText;
    var actionUrl = ViewBag.ActionUrl;
}

<div class="empty-state">
    <div class="empty-state-icon">@icon</div>
    <h3 class="empty-state-title">@title</h3>
    <p class="empty-state-description">@description</p>
    
    @if (actionText != null && actionUrl != null)
    {
        <a href="@actionUrl" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> @actionText
        </a>
    }
</div>
